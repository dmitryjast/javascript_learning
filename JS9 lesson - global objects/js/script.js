var abc = 5;

// Если писать без var, переменная всегда будет записыааться в глобальный объект window

// Переменные всегда должны быть с приставкой (директивой) var

// Тогда она будет храниться внутри и не засорять глобальную область видимости window (своего рода глобальный контекст)